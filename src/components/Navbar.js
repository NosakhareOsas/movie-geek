import { NavLink } from "react-router-dom";

function Navbar (){
    return(
        <div>
            <NavLink to='/movies'>Home</NavLink>
            <NavLink to='/movies'>Home</NavLink>
            <NavLink to='/movies'>Home</NavLink>
        </div>
    );
}

export default Navbar;